# Python requirements for Face Detection ROS Package

# Core dependencies
opencv-python>=4.5.0
numpy>=1.19.0

# Emotion detection dependencies
deepface>=0.0.79
tensorflow>=2.10.0
keras>=2.10.0

# GUI dependencies
Pillow>=8.0.0

# Optional dependencies for enhanced functionality
# matplotlib>=3.3.0  # For additional visualization capabilities
# scipy>=1.5.0       # For advanced image processing

# Note: ROS dependencies should be installed via apt:
# sudo apt-get install ros-$ROS_DISTRO-cv-bridge
# sudo apt-get install ros-$ROS_DISTRO-image-transport
# sudo apt-get install ros-$ROS_DISTRO-sensor-msgs
# sudo apt-get install ros-$ROS_DISTRO-std-msgs
# sudo apt-get install ros-$ROS_DISTRO-geometry-msgs
